services:
  microcredit-frontend:
    container_name: microcredit-frontend
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_GA_MEASUREMENT_ID: ${VITE_GA_MEASUREMENT_ID:?VITE_GA_MEASUREMENT_ID is required}
    restart: unless-stopped
    ports:
      - "8081:80"
